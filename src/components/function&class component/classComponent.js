//rcc command
import React, { Component } from 'react';
// DefaultExport, {Named exports}
// Defaultexport name can be changed to anything(myReact) and be used in code.

export default class classComponent extends Component {
  render() {
    return (
      <div>classComponent</div>
    )
  }
}
//console.log(myReact);
